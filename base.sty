\ProvidesPackage{base}

\usepackage[utf8]{inputenc}
\usepackage[ngerman]{babel}
\usepackage[T1]{fontenc}
\usepackage{lmodern}
\usepackage{mathtools}
\usepackage{amsmath}
\usepackage{amsthm}
\usepackage{amssymb}
\usepackage{cancel}
\usepackage[shortlabels]{enumitem}
\usepackage[parfill]{parskip} % No indentation for new paragraph
\usepackage{pgfplots}
\usepackage{ifthen}
\usepackage{hyperref}
\usepackage{wasysym}

\pgfplotsset{
    compat = 1.15,
    sequence axis/.style = {
        xlabel = {$n \in \IN$},
        ylabel = {$a_n$},
        axis x line = bottom,
        axis y line = left,
        axis line style = {->},
        grid = both,
        xtick distance = 1
    },
    sequence plot/.style = {
        mark = x,
        mark size = 4pt,
        mark options = solid,
        style = dotted
    }
}

\tikzset{
    dot/.style = {
        circle,
        fill,
        inner sep = 1.5pt
    }
}
\usetikzlibrary{pgfplots.fillbetween, patterns, tikzmark, angles, quotes, babel}

\DeclarePairedDelimiter\abs{\lvert}{\rvert}
\DeclarePairedDelimiter\norm{\lVert}{\rVert}
\DeclarePairedDelimiter\qt{\lparen}{\rparen}

% Swap the definition of \abs* and \norm*, so that \abs
% and \norm resizes the size of the brackets, and the 
% starred version does not.
\makeatletter
\let\oldabs\abs
\def\abs{\@ifstar{\oldabs}{\oldabs*}}

\let\oldnorm\norm
\def\norm{\@ifstar{\oldnorm}{\oldnorm*}}

\let\oldqt\qt
\def\qt{\@ifstar{\oldqt}{\oldqt*}}
\makeatother

% Tikzmarker scoping
\newcounter{TikzMarkerPicture}
\newcommand{\newtikzmark}[1]{
    \stepcounter{TikzMarkerPicture}
    \tikzset{tikzmark suffix={-\theTikzMarkerPicture}}
}

\newcommand{\imp}{\Rightarrow}
\newcommand{\eqv}{\Leftrightarrow}
\newcommand{\xor}{\text{ XOR }}
\newcommand{\id}{\text{id}}
\newcommand{\grad}{\text{grad}}
\newcommand{\cotan}{\text{cotan}}

\newcommand{\IN}{\mathbb{N}}
\newcommand{\IR}{\mathbb{R}}
\newcommand{\IQ}{\mathbb{Q}}
\newcommand{\IZ}{\mathbb{Z}}

\newcommand{\BigO}{\mathcal{O}}

\setlength{\mathindent}{0cm}

